# Include variables
include './10-variables.conf'

# Key bindings for taking screenshots
#
# The image files will be written to XDG_SCREENSHOTS_DIR if this is set
# or defined in user-dirs.dir, or to a fallback location XDG_PICTURES_DIR.
#
# Copy the file to ~/.config/sway/config.d/60-bindings-screenshot.conf (or to
# your $XDG_CONFIG_HOME location if set differently) to be able to overwrite
# existing shortcuts.
# Check 'man $screenshot' for additional commands that you may find useful.
#
# Requires:     grimshot

bindsym {
    # Capture the currently active output
    Print       exec $screenshot save output
    $super+p      exec $screenshot save output
    # Capture the currently active window
    Alt+Print   exec $screenshot save active
    $super+Shift+p  exec $screenshot save active
    # Select and capture a custom rectangular area
    Ctrl+Print  exec $screenshot save area
    $super+Ctrl+p exec $screenshot save area
}

