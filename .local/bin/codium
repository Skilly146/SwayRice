#!/bin/sh
# distrobox_binary
# name: development
if [ -z "${CONTAINER_ID}" ]; then
	exec "/usr/bin/distrobox-enter"  -n development  --  '/usr/bin/codium'  "$@"
elif [ -n "${CONTAINER_ID}" ] && [ "${CONTAINER_ID}" != "development" ]; then
	exec distrobox-host-exec '/home/landond/.local/bin/codium' "$@"
else
	exec '/usr/bin/codium' "$@"
fi
